// 출시 전/후로 기능을 켜고 끄는 피처 플래그.
// 코드는 그대로 두고 노출만 제어 → 추후 true 로 바꾸면 즉시 복구(OTA 가능).

/**
 * 컨디션 측정(손가락 탭·반응속도 측정) 기능 노출 여부.
 * 출시 시점엔 입력/설정 항목을 줄이기 위해 숨김(false).
 * - 앱 진입점: 몸상태 화면의 "컨디션 측정하기/측정 기록 보기" 버튼, 복용 후 측정 권유 모달,
 *   메뉴의 "컨디션 측정 기록 보기 / ○○님 컨디션 보기", 설정의 "컨디션 측정 완료 시" 알림.
 * - 측정 화면(TapGame/MeasurementMenu 등)은 등록만 유지되고 진입로가 없어 도달 불가.
 */
export const MEASUREMENT_FEATURE_ENABLED = false;
